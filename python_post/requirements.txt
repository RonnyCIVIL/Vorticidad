# ============================================================
# DEPENDENCIAS PYTHON — Simulación CFD Tornado
# ============================================================
# Instalación:
#   pip install -r requirements.txt
#
# Nota: Para aceleración GPU, instalar CuPy (opcional):
#   pip install cupy-cuda12x  (según versión CUDA)
# ============================================================

# Cálculo numérico (vectorizado con C/Fortran bajo el capó)
numpy>=1.26.0
scipy>=1.12.0

# Visualización 3D interactiva (renderizado en browser)
plotly>=5.18.0

# Visualización científica 3D offline (VTK wrapper)
pyvista>=0.44.0

# Animaciones y plots estáticos
matplotlib>=3.8.0
Pillow>=10.0.0   # Para guardar GIF

# Para leer resultados de OpenFOAM (mallas VTK, VTU)
meshio>=5.3.0

# Para interfaz web API
flask>=3.0.0
flask-cors>=4.0.0

# Opcionales para mayor rendimiento
# cupy-cuda12x  # Aceleración GPU (si tienes CUDA)
# numba>=0.59.0  # JIT compilation
